= render "user/shared/header"
%br
%h2 新規作成
#word_error
  = render 'layouts/error_message', model: @new_word
= form_with model: @new_word do |f|
  = f.text_field :word
  %br
  = f.text_area :content
  %br
  = f.submit "登録"
